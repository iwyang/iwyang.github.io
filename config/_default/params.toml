mainSections = ["post","shuoshuo"]
rssFullContent = true
favicon        = "img/avatar.png"

# 👇 ===== 新增的日期格式配置 ===== 👇
[dateFormat]
    published = "2006-01-02" # 这样会显示为 2024-04-06 (带前导零更好看)
    # 如果你坚持严格要 "2024-4-6"（不带前导零），请把上一行改成 published = "2006-1-2"
    lastUpdated = "2006-01-02 15:04"
# 👆 ================================ 👆

[footer]
    since = 2020

[sidebar]
    emoji    = "🍥"
    subtitle = "博观而约取，厚积而薄发"
    avatar   = "img/avatar.png"

[article]
    readingTime = true  # 确保开启阅读时间/相关元数据功能
    [article.license]
        enabled = false
        default = "Licensed under CC BY-NC-SA 4.0"
	[article.edit]
        enabled = true	

[widgets]
    homepage = [
        { type = "search" },
        { type = "archives", params = { limit = 5 } },
        { type = "categories", params = { limit = 10 } },
        { type = "tag-cloud", params = { limit = 10 } },
    ]
    page = [{ type = "toc" }]

[comments]
    # See complete configuration in config/_default/params.toml
    enabled  = true
    provider = "twikoo"
    [comments.twikoo]
	    # envId: https://twikoo-lake.vercel.app
        envId = "https://t.bore.vip"
        # region =
        # path =
        # lang =